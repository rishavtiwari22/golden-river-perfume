{
  "name": "golden-river-perfume",
  "version": "1.0.0",
  "description": "Full-stack luxury perfume brand website",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "start:client": "cd client && npm start",
    "start:server": "cd server && npm run dev",
    "build:client": "cd client && npm run build",
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"yellow,cyan\" \"cd server && npm run dev\" \"cd client && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {}
}
